# MR.N Local Agent

Local, single-user web automation agent.

## Quickstart

1. Copy `.env.example` to `.env` and set `OPENAI_API_KEY` (and optional `OPENAI_MODEL`).
2. Run `docker compose -f infra/docker-compose.yml up --build`.
3. Open `http://localhost:3000`.

